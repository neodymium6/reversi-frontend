<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <defs>
    <!-- Darker green gradient -->
    <radialGradient id="bgGradient">
      <stop offset="0%" stop-color="#2d7a4a"/>
      <stop offset="100%" stop-color="#1e5433"/>
    </radialGradient>
    
    <!-- White stone -->
    <radialGradient id="whiteStone">
      <stop offset="20%" stop-color="#ffffff"/>
      <stop offset="100%" stop-color="#e5e5e5"/>
    </radialGradient>
    
    <!-- Black stone -->
    <radialGradient id="blackStone">
      <stop offset="20%" stop-color="#4a4a4a"/>
      <stop offset="100%" stop-color="#0a0a0a"/>
    </radialGradient>
    
    <!-- Soft shadow -->
    <filter id="shadow">
      <feDropShadow dx="0" dy="2" stdDeviation="3" flood-opacity="0.4"/>
    </filter>
  </defs>
  
  <!-- Smaller green circle in center -->
  <circle cx="32" cy="32" r="22" fill="url(#bgGradient)" filter="url(#shadow)"/>
  
  <!-- Black stone (top-left, behind, overlapping with white stone) -->
  <circle cx="24" cy="24" r="13" fill="url(#blackStone)" filter="url(#shadow)"/>
  <circle cx="24" cy="24" r="13" fill="none" stroke="rgba(255,255,255,0.15)" stroke-width="0.5"/>
  <ellipse cx="21.5" cy="21" rx="4" ry="3.5" fill="rgba(255,255,255,0.2)"/>
  
  <!-- White stone (bottom-right, in front, overlapping with black stone) -->
  <circle cx="40" cy="40" r="13" fill="url(#whiteStone)" filter="url(#shadow)"/>
  <circle cx="40" cy="40" r="13" fill="none" stroke="rgba(0,0,0,0.08)" stroke-width="0.5"/>
  <ellipse cx="37.5" cy="37" rx="4.5" ry="4" fill="rgba(255,255,255,0.6)"/>
</svg>